import 'bootstrap.dart';

/// Entry point.
///
/// Startup logic lives in [bootstrap] so it can be reused by integration tests
/// and by any future flavour-specific entry point.
void main() => bootstrap();
